// metodos e objetos 

// uma função que retorna ou não algum valor

var pessoa = {
    altura: 1.50,
    peso: 60,
    andar(){
        return "Estou andando"
    }
}

console.log(pessoa.andar())
 
// função quando está fora do objeto / método quando está dentro do objeto
// variável quando está fora do objeto / atributo|propriedade quando está dentro do objeto

